#include "linked-list.h"
#include "queue.h"
#include "stack.h"
#include "tree.h"
#include <iostream>
using namespace std;

int main(){
	LinkedList a;
	a.pushback(1);
	a.pushback(2);
	a.pushback(3);
	a.popback();
	a.popfront();



}
